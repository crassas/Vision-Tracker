# Cloudflare Pages deployment.
#   npm run build && npx wrangler pages deploy dist --project-name vision-tracker
name = "vision-tracker"
compatibility_date = "2025-09-01"
pages_build_output_dir = "dist"
