# Optional: self-host the MediaPipe model bundles (e.g. on Cloudflare R2) instead
# of fetching them from Google's public model bucket. Must mirror the same paths.
# VITE_MODEL_BASE=https://models.example.workers.dev
